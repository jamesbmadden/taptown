/**
 * In a dev build, this file will be blank, therefore resulting in no files caching,
 * but in production, the list of files will be generated at build time.
 * appVer is the time when the app was compiled, used a simple way of versioning.
 * Development builds use a negative number
 */
var appVer = -1;

var filesToCache = [

];